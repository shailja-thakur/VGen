// Design a module that implements an AND gate

module and_gate( 
    input a, 
    input b, 
    output out );

    // assing the AND of a and b to out.
    
and_gate
    a, 
    b, 
    out;

// Design a class that implements an OR gate

class or_gate {

    // the OR of a and b to out

    public void or(
        input a, 
        input b, 
        output out );

    // assign the OR to out
    
    void or
        a, 
        b, 
        out;

}

// Design a class that implements an XOR gate

class xor_gate {

    // the XOR of a and b to out

    public void xor(
        input a, 
        input b, 
        output out );

    // assign the XOR to out
    
    void xor
        a, 
        b, 
        out;

}

// Design a class thatendmodule
