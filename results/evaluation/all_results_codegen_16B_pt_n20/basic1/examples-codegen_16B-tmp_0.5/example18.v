// Design a module that assigns the output to the input
module wire_assign( input in, output out );
// assign out to in
wire_assign( in, out );

// Design a module that implements a counter
// that counts down until it reaches zero
module counter( input in, output out );
// Initialize counter with value
counter( in, out );
// Decrement counter
counter( in, out );

// Design a module that implements a counter
// that counts up until it reaches zero
module counter( input in, output out );
// Initialize counter with value
counter( in, out );
// Increment counter
counter( in, out );

// Design a module that implements a counter
// that counts up until it reaches zero
module counter( input in, output out );
// Initialize counter with value
counter( in, out );
// Increment counter
counter( in, out );

// Design a module that implements a counter
// that counts up until it reaches zero
module counter( input in,endmodule
