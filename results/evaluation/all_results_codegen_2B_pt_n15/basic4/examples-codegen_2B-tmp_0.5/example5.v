// Design a 2-to-1 multiplexer.
module mux( 
input [4:0] a, b,
input sel,
output [4:0] out );
// When sel=0, assign a to out. 
// When sel=1, assign b to out.

// Multiplexer for a 4-bit input.
module multiplexer( 
input [4:0] a, 
output [4:0] out );
// Assign a to out.

// Multiplexer for a 4-bit input.
module multiplexer2( 
input [4:0] a, 
output [4:0] out );
// Assign a to out.

// Multiplexer for a 4-bit input.
module multiplexer3( 
input [4:0] a, 
output [4:0] out );
// Assign a to out.

// Multiplexer for a 4-bit input.
module multiplexer4( 
input [4:0] a, 
output [4:0] out );
// Assign a to out.

// Multiplexer for a 4endmodule
