// Design a module that assigns the output to the input
module wire_assign( input in, output out );
// assign out to in
module wire_assign( input in, output out ) {
	out = in;
}
// Design a module that divides the input by 2
module wire_divide( input in, output out ) {
	out = in / 2;
}
// Design a module that multiplies the input by 2
module wire_multiply( input in, output out ) {
	out = in * 2;
}
// Design a module that adds the input to the input
module wire_add( input in, output out ) {
	out = in + in;
}
// Design a module that subtracts the input from the input
module wire_subtract( input in, output out ) {
	out = in - in;
}
// Design a module that prints the input
module wire_print( input in, output out ) {
	out = in;
}
// Design aendmodule
