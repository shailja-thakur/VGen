// This is a half adder. A half adder adds two bits and produces a sum and carry-out.
module half_adder( 
input a, b,
output cout, sum );

half_adder(
input a, b,
output cout, sum
);

// This is a full adder. A full adder adds two bits and produces a sum and carry-out.
module full_adder( 
input a, b, c,
output cout, sum, carry );

full_adder(
input a, b, c,
output cout, sum, carry
);

// This is a full adder. A full adder adds two bits and produces a sum and carry-out.
module full_adder_2( 
input a, b, c,
output cout, sum, carry );

full_adder_2(
input a, b, c,
output cout, sum, carry
);

// This is a full adder. A full adder adds two bits and produces a sum and carry-out.
module full_adder_3endmodule
