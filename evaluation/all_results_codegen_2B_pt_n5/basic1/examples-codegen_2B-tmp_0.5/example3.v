// Design a module that assigns the output to the input
module wire_assign( input in, output out );
// assign out to in

// Design a module that adds two numbers
module adder( input a, input b, output add );
// add two numbers together

// Design a module that multiplies two numbers
module multiplier( input a, input b, output multiply );
// multiply two numbers together

// Design a module that subtracts two numbers
module subtr( input a, input b, output sub );
// subtract two numbers

// Design a module that divides two numbers
module div( input a, input b, output div );
// divide two numbers

// Design a module that prints the numbers
module print_numbers( input in, output out );
// print the numbers

// Design a module that adds the numbers
module add_numbers( input in, output out );
// add the numbers

// Design a module that subtracts the numbers
module sub_numbers( input in, output out );
// subtractendmodule
