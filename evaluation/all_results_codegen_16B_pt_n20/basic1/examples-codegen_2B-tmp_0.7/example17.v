// Design a module that assigns the output to the input
module wire_assign( input in, output out );
// assign out to in
module wire_assign() {
  wire foo;
  assign foo = in;
  wire bar;
  assign bar = in;
};

// Design a module that assigns the output to the input
module wire_assign_2( input in, output out );
// assign out to in
module wire_assign_2() {
  wire foo;
  assign foo = in;
  wire bar;
  assign bar = in;
};

// Design a module that assigns the output to the input
module wire_assign_3( input in, output out );
// assign out to in
module wire_assign_3() {
  wire foo;
  assign foo = in;
  wire bar;
  assign bar = in;
  wire boo;
  assign boo = in;
};

// Design a module that assignsendmodule
