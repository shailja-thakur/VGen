// This is a half adder. A half adder adds two bits and produces a sum and carry-out.
module half_adder( 
input a, b,
output cout, sum );

// This is an adder. An adder adds two bits and produces a sum and carry-out.
module adder( 
input a, b,
output cout, sum );

// This is a subtractor. A subtractor subtracts two bits and produces a sum and carry-out.
module subtractor( 
input a, b,
output cout, sum );

// This is an multiplier. A multiplier multiplies two bits and produces a sum and carry-out.
module multiplier( 
input a, b,
output cout, sum );

// This is a divider. A divider divides two bits and produces a sum and carry-out.
module divider( 
input a, b,
output cout, sum );

// This is a modulo. A modulo divides two bits and produces a sum and carry-out.
module modulo( 
input aendmodule
