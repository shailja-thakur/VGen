// Design a 2-to-1 multiplexer.
module mux( 
input [4:0] a, b,
input sel,
output [4:0] out );
// When sel=0, assign a to out. 
// When sel=1, assign b to out.
// mux(a, b, sel, out);
// a is selected if sel=0.
// b is selected if sel=1.
// assign out = (sel==0) ? a : b;
// assign out = (sel==0) ? a : b;
// assign out = (sel==0) ? a : b;
// assign out = (sel==0) ? a : b;
// assign out = (sel==0) ? a : b;
// assign out = (sel==0) ? a : b;
// assign out = (sel==0) ? a : b;
// assign out = (sel==0) ? a : b;
// assign out = (sel==0) ? a : b;
// assign out = (sel==0) ? a : b;
// assign out = (sel==0) ? a : bendmodule
