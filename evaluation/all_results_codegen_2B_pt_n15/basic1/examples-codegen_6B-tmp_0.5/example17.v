// Design a module that assigns the output to the input
module wire_assign( input in, output out );
// assign out to in
wire_assign(in, out);

// Design a module that calculates the average of the values in the input
module average( input in, output avg );
// calculate the average of the values in the input
average(in, avg);

// Design a module that calculates the sum of the values in the input
module sum( input in, output sum );
// calculate the sum of the values in the input
sum(in, sum);

// Design a module that calculates the minimum of the values in the input
module min( input in, output min );
// calculate the minimum of the values in the input
min(in, min);

// Design a module that calculates the maximum of the values in the input
module max( input in, output max );
// calculate the maximum of the values in the input
max(in, max);

// Design a module that calculates the product of the values in theendmodule
