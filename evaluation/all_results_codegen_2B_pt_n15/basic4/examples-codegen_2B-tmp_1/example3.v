// Design a 2-to-1 multiplexer.
module mux( 
input [4:0] a, b,
input sel,
output [4:0] out );
// When sel=0, assign a to out. 
// When sel=1, assign b to out.
squelch squel;
buffer x;

wire [3:0] st, led;

assign st = foo;

prog top;

dcl trigger;

trigger __TRIGGER;

initial begin
   digital_4m st1;
   XOR        st, st1;
end

always @(*) begin
   trigger = );
end


// ???
assign led [4:0] = 1'b0;

#pragma vector= allen_matrix
interrupt request( bit64a *!bits );
// A bit64 request is sent over all 64 counters to interrupting this is wired up to the terminal logical bus(s).

void ITESter()
  entered; 
enable unprecedence mark( if ( (st == lower) && (!bits [1] || bits [2])endmodule
