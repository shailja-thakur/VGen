// Design a module that assigns the output to the input
module wire_assign( input in, output out );
// assign out to in
out = in;
// $ Underscore.js 2.4.1 http://underscorejs.org
helper = {
  negate: function( x ) {
    return -( x + 1 ) + x;
  },
  toRad: function( x ) {
    return x * Math.PI / 180;
  }
}
Module class parasitic( $Module.Module, {} );
this.helperName = $helperName;
$WebkitModule = $Module.Module( {
  name: $webkitModuleName
} );
$BMapModule = $Module.Module( {
  name: $BMapModuleName
} );
$AreaMapModule = $Module.Module( {
  name: $AreaMapModuleName
} );
__nop__.Variable.__vc = _.clone( $Variable.Variable )
endmodule
